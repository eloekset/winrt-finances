// This code lets the application handle share target operations.

(function () {
    "use strict";

    /// <summary>
    /// Handles the activated event executed on activation of the share target.
    /// </summary>
    /// <param name="eventArgs">
    /// Arguments of the event. In the case of the Share contract, it has the ShareOperation.
    /// </param>
    function handleShareTargetActivatedEvent(eventArgs) {

    }
})();